using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using ProjetTexcel_WEB.Models;

namespace ProjetTexcel_WEB.Controllers
{
    public class UserManager
    {
        /*public bool IsValid(string username, string password)
        {
            using (var db = new bdTexelFredAlexEntities()) // use your DbConext
            {
                // if your users set name is Users
                return db.tblUser.Any(u => u.username == username
                    && u.pass == password);
            }
        }*/
    }
}